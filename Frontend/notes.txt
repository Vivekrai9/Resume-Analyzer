4 layer architecture 


(1. UI)  //user ko kya dikhana hai or user kya dekhega + navigation maintain krna
=> Component
=> pages


(2. Hook) ===> for managing state and api layers
=> hooks
    ==> useAuth.js
        ==> ( handllogin , handleRegister, handleLogout )  these are three function.

(3. state) ===> means data store  /* ye hume batata ki hai jo UI pr data dikhana hai vo bhi kahi na kahi toh store krna padega, like login me username ki jagah username dikhana hai or user ne hume koon koon si job description diya hai
                                     Basically, data ki jo state manage kr rahi hoti hai usse ye layer manage krta hai
                                  */

                                    /* ak user logged-in hai th uska data store karwana hai...tb tk jo user ne api ko request ki hai usme time lgta hai toh utne der tk loading bhi dikhana hai..then dono kaam ko ye STATE LAYER handle krti hai 
                                    */
=> auth.context.jsx
=> ai.context.jsx



(4. Api)  ===> for communiction with backend // iska kaam hota hai that how the frontend communicate with the backend
    => Backend 
        => auth.routes.js // isme hume 4 api banae hai 
            ==> register
            ==> login
            ==> logout
            ==> get-me 

    => src/features/auth/
        => services  // isme folder hum log code likhte hai that how frontend files interact with the backend api 
                     // for communication frontend with backend iske liye ak package ki need hoti hai and that package is axios(npm i axios)
            => auth.api.jsx   





            
